import React, { FC } from "react";

const Footer: FC = (props) => {
  return (
    <footer className="container mx-auto px-3">
      <div className="mx-auto px-32 py-6 text-gray-600 leading-8">
        {/* TODO: - Disabled footer for now, but should plan to make a better footer... */}
        {/* Made by Alex */}
        &nbsp;
      </div>
    </footer>
  );
};

export default Footer;
